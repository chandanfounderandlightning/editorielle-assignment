export type User = {
  first_name: string;
  last_name: string;
  email: string;
};

export type UserCamelCase = {
  firstName: string;
  lastName: string;
  email: string;
};

export type pauseDate = {
  pause_till: string;
};